import 'package:flutter/material.dart';

class EditPositionPage extends StatelessWidget {
  const EditPositionPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        // child: child,
        );
  }
}
